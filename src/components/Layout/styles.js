import styled from 'styled-components';

const Container = styled.div`
  strong {
    font-size: 30px;
    color: var(--black);
  }

  button {
    width: 100px;
    height: 50px;
    color: #fff;
    cursor: pointer;
  }
`;

export default Container;
